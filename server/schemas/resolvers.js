const { AuthenticationError } = require('apollo-server-express');
const { User } =require('./../models');
const { signToken } = require('./../utils/auth');

const resolvers = {
    //deal with resolvers later
}

module.exports = resolvers;
